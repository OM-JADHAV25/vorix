spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vorix_project_test_db
    username: username
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: false

    properties:
      hibernate:
        format_sql: false